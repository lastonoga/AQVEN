apiVersion: "aqven/v1"
kind: "Project"
description: "Модуль в рекомендуемой раскладке: у каждого вида одно место"
package: "layout_shop"
providers:
- id: "openai"
  api_key: "ref:env/OPENAI_API_KEY"
  data_policy:
    allows_pii: true
    allows_sensitive: false
    retention: "zero"
