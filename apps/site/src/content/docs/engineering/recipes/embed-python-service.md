---
title: Embed a Workflow in a Python Service
description: Load a project from Python, start a typed flow, and retain runtime evidence for the calling service.
---

Load the project from its root, use generated input types at the service boundary, start the declared flow, and handle a result, wait, or failure explicitly. Keep server transport concerns outside the flow definition. Use the local project server when another process needs REST events or Studio.

Read [Python API](/engineering/python-api/) and the generated [Python API Reference](/engineering/reference/python-api/).
