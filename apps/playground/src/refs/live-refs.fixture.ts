export const LIVE_REFS: readonly string[] = [
  "$advance.out",
  "$agreement.out.level",
  "$answer.out",
  "$auto.out",
  "$board.out",
  "$brief.out",
  "$brief.out.facts",
  "$candidate.out",
  "$candidates.out",
  "$cheap.out",
  "$check_citations.out",
  "$check_refs.out",
  "$check_refs.out.status",
  "$chosen.out",
  "$cited.out",
  "$classify.out",
  "$classify.out.route",
  "$close_items.out.allDone",
  "$close_items.out.checklist",
  "$close_items.out.iterations",
  "$close_items.out.state",
  "$completion.out.status",
  "$consensus.out",
  "$consensus.out.agreement",
  "$consensus.out.agreement.level",
  "$criticize.out",
  "$decide.out",
  "$deduped.out",
  "$digest.out",
  "$digest.out.answer",
  "$digest.out.candidate",
  "$digest_call.out",
  "$draft.out",
  "$drafts.out",
  "$extract.out",
  "$final_answer.out",
  "$final_email.out",
  "$final_state.out",
  "$fix.out.candidate",
  "$fix.out.issues",
  "$gate.out",
  "$gate.out.decision",
  "$gate.out.level",
  "$generator",
  "$grounding.out",
  "$grounding.out.decision",
  "$hard_cases.out",
  "$ideate.out",
  "$in.candidate",
  "$in.checklist",
  "$in.checklist[*].id",
  "$in.critique",
  "$in.dedupKey",
  "$in.feedback",
  "$in.items",
  "$in.n",
  "$in.overrides",
  "$in.rubric",
  "$in.state",
  "$in.strategy",
  "$in.task",
  "$in.task.facts[*].id",
  "$in.task.sections[*].ticketIds",
  "$in.task.sources[*].id",
  "$in.task.tickets[*].id",
  "$in.ticket",
  "$in.vary",
  "$input",
  "$input.accountId",
  "$input.audience",
  "$input.batch_id",
  "$input.caseId",
  "$input.catalog.currencies",
  "$input.catalog.currencies[*].code",
  "$input.categories",
  "$input.categories[*].id",
  "$input.checklist",
  "$input.company",
  "$input.document",
  "$input.locale",
  "$input.name",
  "$input.offer",
  "$input.options[*].id",
  "$input.order",
  "$input.order.items[*].id",
  "$input.owner",
  "$input.text",
  "$input.ticket",
  "$input.tier",
  "$input.topK",
  "$input.topic",
  "$input.version",
  "$item",
  "$iter.allDone",
  "$iter.clean",
  "$iter.critique.meetsThreshold",
  "$iter.valid",
  "$kb.out",
  "$kb.out[*].id",
  "$labels.out",
  "$load_account.out",
  "$load_lead.out",
  "$load_policies.out",
  "$load_policies.out[*].id",
  "$load_tickets.out",
  "$merged.out",
  "$onboarding_task.out",
  "$outreach_task.out",
  "$pair.out",
  "$panel.out",
  "$panel.out.decision",
  "$pick.out.best",
  "$policy.out",
  "$polish.out.candidate",
  "$polish.out.critique",
  "$polish.out.iterations",
  "$polish.out.score",
  "$quality.out",
  "$recheck.out",
  "$reconcile.out",
  "$release_task.out",
  "$render.out",
  "$reply.out",
  "$report.out",
  "$resolve.out",
  "$result.out",
  "$retrieve.out",
  "$retrieve.out[*].id",
  "$revise.out",
  "$rewrite.out",
  "$routed.out",
  "$score",
  "$score.out",
  "$scores.out",
  "$solutions.out",
  "$stats.out",
  "$tally.out",
  "$tech_route.out.result",
  "$tickets.out",
  "$tickets.out[*].id",
  "$triage.out",
  "$used_items.out",
  "$validate.out",
  "$variants.out",
  "$vendors.out",
  "$vendors.out[*].id",
  "$verify.out",
  "$voted.out",
  "$write.out",
]
