---
title: Combine Deterministic Code and a Model
description: Normalize input in code, use a model for judgment, then validate or format deterministically.
---

Use a code node before the model for parsing, normalization, or required lookup. Bind only the prepared values into an inference. Use a code node after the model for deterministic validation, conversion, or action selection. Keep a dataset case that proves the handoff between the two boundaries.

Read [Code Nodes](/engineering/code-nodes/) and [Model Nodes](/engineering/model-nodes/).
