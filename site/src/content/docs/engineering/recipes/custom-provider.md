---
title: Add a Custom Provider
description: Add a code-backed Pydantic AI model route with declared capability and data policy.
---

Create a `kind: code` provider declaration, implement the model factory, store credentials through secret references, and declare only capabilities that the route supports. Run `models check`, then a representative live request and evaluation before enabling the route in a production agent.

Read [Custom Providers](/engineering/custom-providers/).
