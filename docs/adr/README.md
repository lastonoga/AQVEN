# Записи архитектурных решений (ADR)

Каждая запись фиксирует одно решение: чем оно вызвано, что именно решено, какие альтернативы
отвергнуты и почему, чем проверяется соблюдение и по какому триггеру решение пересматривается.

Решения уже сведены в [../DECISIONS.md](../DECISIONS.md) — этот каталог объясняет, **почему**
они такие. Если документ комплекта противоречит ADR — ошибка в документе.

| ADR | Решение | Ключевой аргумент |
|---|---|---|
| [0001](0001-execution-core-on-voltagent.md) ⚠️ заменено 0025 | Своё ядро исполнения поверх VoltAgent | 19 из 20 конструкций IR ложатся на готовые примитивы; нашим остаётся только периметр гарантий |
| [0002](0002-ai-sdk-v6-axis.md) ⚠️ заменено 0025 | Ось AI SDK v6, провайдеры изолированы в `@aqven/llm` | Опасно не столкновение версий, а его отсутствие: у `@ai-sdk/*` нет peer на `ai`, поэтому npm молча ставит линию v7 и ломается уже в рантайме |
| [0003](0003-model-middleware.md) ⚠️ заменено 0029 | Гарантии живут в middleware уровня модели | `Agent.generateObject` помечен deprecated; кассеты, бюджеты и PII-редакция нужны на каждом вызове, а не в обёртке одного метода |
| [0004](0004-schema-profiles.md) ⚠️ частично изменено 0029 | Профиль схемы — свойство пары (IR-схема × провайдер) | Требования OpenAI и Anthropic взаимно противоречивы; Zod 4 даёт пять проверенных поломок strict-вывода |
| [0005](0005-three-call-outcomes.md) ⚠️ частично изменено 0029 | Три исхода вызова, обрезанный ответ чинить запрещено | `jsonrepair` на обрыве дописывает `null` и отдаёт валидный, но ложный объект |
| [0006](0006-dynamic-allowed-sets.md) ⚠️ частично изменено 0027, 0029 | Потолок allowed-set и индексный выбор | Ёмкость упирается в формат ID: UUID v4 — 416 значений, nanoid — 714; выше падает и точность |
| [0007](0007-single-operation-contract.md) ⚠️ заменено 0028 | Один контракт операций: Zod → OpenAPI → MCP и клиент UI | `@voltagent/server-hono` уже вендорит `@hono/zod-openapi`; tRPC был бы вторым транспортом рядом |
| [0008](0008-mcp-tool-naming-and-phasing.md) ⚠️ частично изменено 0025 | `snake_case` имена тулов и фазовое раскрытие | Точка в имени невалидна; 50 тулов стоят 12–30 тыс. токенов до первого действия |
| [0009](0009-op-log-not-crdt.md) | Server-authoritative op-log вместо CRDT | CRDT гарантирует сходимость, но не инварианты графа: реплики сойдутся к привязке на удалённый узел |
| [0010](0010-pg-boss-scheduler.md) ⚠️ заменено 0025 | pg-boss как планировщик и единственная очередь | Postgres уже есть; транзакционная постановка задачи вместе с бизнес-транзакцией снимает свой outbox |
| [0011](0011-two-schemas-and-tenancy.md) ⚠️ частично изменено 0025 | Две схемы в базе, мультитенантность в данных с первого дня | VoltAgent создаёт свои таблицы в рантайме; добавить `tenant_id` позже дороже, чем завести сразу |
| [0012](0012-langfuse-as-store.md) ⚠️ частично изменено 0025 | Langfuse — хранилище, отладчик — свой | Отладчик показывает наш IR, а не спаны; всё, от чего зависит поведение, дублируется в наш Postgres |
| [0013](0013-liquidjs-template-engine.md) ⚠️ заменено 0029 | liquidjs как движок шаблонов | Встроенный статический анализ и типизированный AST условий; `{% switch %}` из спеки не нужен — есть `{% case %}` |
| [0014](0014-ir-mutation-testing.md) ⚠️ частично изменено 0025 | Доменный IR-мутатор для kill-критерия «100% мутантов» | Stryker мутирует исходники, а ловить надо типовые ошибки агента в IR |
| [0015](0015-gate-statistics.md) ⚠️ заменено 0029 | Своя статистика гейтов на `@stdlib`, без Python | Трёх пакетов и ~150 строк хватает на McNemar, бутстрап BCa и поправки на множественность |
| [0016](0016-git-as-release-boundary.md) ⚠️ частично заменено 0017 | Git — граница релиза | Исследование: ни один из семи продуктов с визуальным редактором не сделал git источником истины; у git нет точки, где слияние можно отклонить |
| [0017](0017-files-as-source-of-truth.md) ⚠️ частично изменено 0025, 0026 | Файлы — источник истины для определений, база — индекс | Основной пользователь работает в Claude Code: определения должны правиться напрямую, а не только через UI |
| [0018](0018-typescript-authoring.md) ⚠️ заменено 0026 | TypeScript — единственный язык авторинга, канвас правит кодмодом | Визуальный редактор Cube отключается на JS-моделях; двусторонний круговорот структуры доказан только в WinForms 2002 |
| [0019](0019-escape-hatch-rules.md) ⚠️ частично изменено 0026, 0029 | Escape hatch даёт значение, но не форму | Cube держит гарантии фазовым разделением: код влияет на значения, не на структуру модели |
| [0020](0020-llm-function-and-adapters.md) ⚠️ частично изменено 0026, 0029 | LLM-функция как единица; адаптация под модель — работа платформы | 17 осей адаптации выводятся из каталога и проб; слово «JSON» вставляет адаптер, а не автор |
| [0021](0021-prompt-dependency-model.md) | Промт собирается в рантайме; идентичность — по факту отправки | Предвычисление влияния отвергнуто как переусложнение: отрисованный промт в трассе даёт те же свойства |
| [0022](0022-hash-as-version.md) ⚠️ частично изменено 0026 | Версия — хеш содержимого, а не поле в исходнике | Ручной номер не проверяем машиной; идентичность и метка релиза — разные вещи |
| [0023](0023-lexical-references.md) ⚠️ заменено 0026 | Узлы связываются лексическими ссылками, а не строками | Вдвое дешевле по типам, спеллчек в ошибках, ацикличность становится невыразимой, кодмод точнее |
| [0024](0024-studio-on-vite.md) ⚠️ частично изменено 0025, 0028 | Studio — SPA на Vite, а не Next.js | Локальный инструмент (ADR-0017) не использует RSC, Server Actions и `proxy.ts`; `aqven dev` раздаёт статику с одного порта |
| [0025](0025-python-engine.md) ⚠️ частично изменено 0030 | Движок на Python: Pydantic AI и DBOS; студия остаётся на TypeScript | Шаг `code` — функция проекта-хозяина в процессе модуля: вызвать её без границы сериализации и проверить pyright strict можно только на том же языке; DBOS — библиотека в процессе, локально на SQLite, без отдельного сервера оркестрации |
| [0026](0026-yaml-spec-and-code-refs.md) ⚠️ частично изменено 0027, 0030 | Описание воркфлоу — YAML на Pydantic-моделях, код по ссылке `module:function`, сборка в wheel | YAML читается как данные без исполнения кода: канвас правит его через `flow_patch` без кодмода, IR становится производным и не коммитится; те же модели дают закрытый набор ключей и JSON Schema для редактора |
| [0027](0027-dynamic-io-shapes.md) | Динамическая форма входа и выхода: пять случаев, тип `Dynamic`, шаг `narrow` | Один механизм «схема из данных» стёр бы статическую проверку типов между узлами, R-T1, стабильный хеш схемы и кеш грамматики провайдера; берётся наименее динамичный случай, который решает задачу |
| [0028](0028-studio-api-contract.md) ⚠️ частично изменено 0030 | API студии: Pydantic → FastAPI и OpenAPI 3.1 → типы TypeScript, SSE, MCP на том же порту | Маршрут и MCP-тул вызывают один use-case и валидируют одной моделью; `aqven dev` и `aqven serve` — один Python-процесс, а TS control plane на Hono описывал бы контракт дважды |
| [0029](0029-trust-and-quality-python.md) ⚠️ частично изменено 0030 | Ядро доверия на Python: цепочка `WrapperModel`, python-liquid, pydantic-evals, статистика гейта на scipy и statsmodels | Дефолты Pydantic AI нарушают три исхода ADR-0005 — обрезанный ответ повторяется или принимается как `ok`, поэтому гейт исходов стоит первым звеном цепочки; математику гейта дают библиотеки, нашей остаётся доменная политика |
| [0030](0030-local-browser-backend.md) ⚠️ частично изменено 0034 | Локальный бэкенд в браузере: пакет `aqven` в окружении проекта, только SQLite, токен на запуск, первоначально чат только на Claude, ключи провайдеров в настройках, каждый вызов модели потоком, узел `llm` сегментными шагами, ветки дочерними workflow; поправки `A1`–`A17` по итогам реализации: ключи в `.env` проекта, все провайдеры через реестр Pydantic AI и свои через один интерфейс `Model`, `output.mode` с детерминированным `auto`, `aqven new`/`dev`, `aqven check` с симулированным прогоном, `aqven prompt preview`, библиотека прежде всего | Код проекта импортирует свои зависимости в процессе движка, поэтому сервер живёт в окружении проекта, а не в замороженном бинарнике; конкурентные шаги в одном DBOS-workflow после восстановления молча подменили выходы веток, дочерние workflow — нет |
| [0031](0031-public-api-docstrings.md) | Docstring разрешён только на публичной поверхности `aqven`/`aqven-llm`, для генерации API-референса сайта документации | `E_DOCSTRING` (ADR-0026 §8) запрещает docstring только у кода проекта, не у движка; griffe — живой экстрактор для Python API-референса, без него описание API пришлось бы дублировать вручную |
| [0032](0032-studio-routes-on-engine-entities.md) | Маршруты студии `/flows/$flowId/{canvas\|nodes\|runs\|datasets\|review\|evals}` без языкового сегмента, словарь домена — зеркало `packages/api-types` | Сегментов `workspace` и `workflow` у движка нет, а каталог `flows/project/` законен по ADR-0026 — статический сегмент `flows` снимает затенение динамического маршрута статическим; эвалы и датасеты остаются экраном и читаются индексом файлов, пока нет своего ресурса API |
| [0033](0033-flow-datasets-and-scoped-runs.md) ⚠️ частично изменено 0035 | Датасеты flow и запуски с выбранными целевыми узлами | Для `selected_nodes` движок исполняет зависимости выбранных узлов; новый диапазон Dataset имеет отдельный контракт |
| [0034](0034-codex-and-project-chat-threads.md) | Codex CLI как второй адаптер чата; выбор бэкенда для новых тредов и общий список разговоров проекта | Нативные истории Claude и Codex остаются раздельными, но используют один проект, журнал и контракт Studio |
| [0035](0035-dataset-stage-ranges.md) | Запуск непрерывного диапазона этапов из Dataset | Фикстуры выходов пропущенных узлов дают тестировать средний этап без запуска начала flow; доступность проверяет движок |
| [0036](0036-single-root-monorepo.md) | Один корень монорепозитория: `apps`, `packages`, `examples`, `tools`, `docs` | Каталоги `aqven-py` и `site` упразднены, корень репозитория — корень обоих workspace; появился CI |
| [0037](0037-studio-inside-the-wheel.md) | Studio едет внутри wheel `aqven`: установка одной командой | Собранный бандл копируется в `aqven/server/static/` отдельным шагом до `uv build`; Node у пользователя не нужен |
| [0038](0038-publishing-to-pypi.md) | Публикация на PyPI: одна версия на два дистрибутива, релиз по тегу, trusted publishing | `aqven` пинит `aqven-llm` точной версией; релиз по тегу `v*` через OIDC, без секретов в репозитории |
| [0039](0039-polyform-shield-license.md) | Лицензия PolyForm Shield 1.0.0 | Продавать AQVEN, в том числе форк, может только владелец; строить на нём и продавать воркфлоу — все |
| [0040](0040-chat-agent-allowed-tools.md) | Разрешения чат-агента задаёт запуск, а не файлы проекта | `--chat-allow-tool` передаётся человеком при старте, поэтому агент не может расширить свои права, правя проект; защита `.env` укреплена против обхода кавычками |

## Как добавлять новую запись

1. Следующий свободный номер, имя файла `NNNN-краткое-имя-латиницей.md`.
2. Структура: Контекст → Решение → Альтернативы → Последствия → Проверка → Пересмотр. Запись может
   заканчиваться разделом «Открытые вопросы» после «Пересмотра»: нумерованный список, у каждого пункта —
   что сделать, чтобы закрыть.
3. Если решение меняет [../DECISIONS.md](../DECISIONS.md) — правьте оба файла одним изменением.
4. Отменённая запись не удаляется: статус меняется на «заменено ADR-NNNN», со ссылкой. Если новая
   запись меняет старую только в части — статус «частично изменено ADR-NNNN». В обоих случаях под
   датой в шапке — абзац о том, что заменено и что остаётся в силе, а в таблице выше — пометка ⚠️.
