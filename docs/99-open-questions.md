# 99. Открытые вопросы комплекта

> Статус: ready
> Зависит от: все документы комплекта 01–22
> Источники: scratchpad/all-open-questions.txt (сбор от 22 авторов), DECISIONS.md, research/00-verified-by-lead.md

Рабочий реестр, а не отчёт. Одна строка — одна единица работы с владельцем и сроком.
Дубли сведены, разрешённое законом (DECISIONS.md и research/00-verified-by-lead.md) вычеркнуто.

Колонка «Док» — номера документов комплекта: `01`=01-product-and-requirements, `02`=02-architecture,
`03`=03-core-language, `04`=04-ir-schema, `05`=05-type-system, `06`=06-registries, `07`=07-compiler,
`08`=08-prompts, `09`=09-context-model, `10`=10-runtime, `11`=11-providers, `12`=12-observability,
`13`=13-evals-and-gates, `14`=14-mcp-contract, `15`=15-studio-frontend, `16`=16-data-model,
`17`=17-jobs-and-scheduling, `18`=18-export-and-conformance, `19`=19-security-and-policies,
`20`=20-repo-and-tooling, `21`=21-roadmap, `22`=22-glossary.

## Сводка числами

| Величина | Значение |
|---|---|
| Вход: строк от авторов | 354 (265 вопросов + 89 противоречий), 22 документа |
| Закрыто законом (DECISIONS.md / 00-verified-by-lead.md / ADR-0017) | 52 (43 противоречия + 9 вопросов) |
| Противоречий разрешено авторами по месту, решения не требуют | 16 |
| Противоречий, продублированных отдельным вопросом (учтены в реестре) | 30 |
| Записей в реестре после дедупликации | 126 (122 от авторов + 4 открыты ADR-0017) |
| Коэффициент сжатия | 2.5× (305 живых строк → 122 записи) |

| Класс | Записей | Оценка усилия |
|---|---|---|
| BLOCKER — без ответа нельзя начинать спайк фазы 0 | 21 | ≈10 человеко-дней (2 инженера параллельно — 5 дней) |
| PHASE-1 — закрыть до фазы 1 | 65 | ≈42–47 человеко-дней |
| LATER — решается по ходу | 27 | ≈19–24 человеко-дня, без дедлайна |
| PRODUCT — решение владельца продукта, не техническое | 13 | решения, не работа |

Ни один BLOCKER не требует внешних согласований: всё закрывается чтением `.d.ts`, пробным прогоном
или однострочным решением ведущего. Самый дорогой — спайк `isolatedDeclarations × Zod 4` (1 день).

## Закрыто законом — в реестр не попадает

| Тема | Где всплывала | Чем закрыто |
|---|---|---|
| Версия @openrouter/ai-sdk-provider (2.10.0 peer ai ^6; несовместима только ветка 3.x) | 01,02,04,06,08,11,12,14,18 | DECISIONS «Ось версий» + уточнение в 00-verified-by-lead |
| Горячая регистрация воркфлоу в рантайме (§20.4 «не подтверждено») | 01,02,10 | 00-verified-by-lead: `WorkflowRegistry.registerWorkflow/unregisterWorkflow` в dist |
| Нужен ли свой replay-кэш (timeTravel переисполняет вызовы моделей) | 02,03,10 | DECISIONS «Детерминизм реплея» |
| Своя таблица lineage форков | 02,10,21 | DECISIONS «Lineage форков» |
| Калибровка судьи одним числом `min_agreement: 0.8` | 01,03,13 | DECISIONS: QWK ≥ 0.7 И Krippendorff alpha ≥ 0.8 |
| `{% switch %}` как кастомный тег | 07,08 | DECISIONS: встроенный liquidjs `{% case %}/{% when %}` |
| Переопределение model/instructions в опциях вызова | 03,10,21 | DECISIONS: только динамические функции агента |
| `on_item_error`, first-match у `andBranch`, `quorum(k)` | 03 | DECISIONS «Что VoltAgent НЕ даёт» |
| Библиотека канонизации (json-canonicalize vs canonicalize@5.0.0) | 13,17 | DECISIONS «Экспорт» |
| @voltagent/langfuse-exporter против @langfuse/otel | 12,19 | DECISIONS «Трассы» (пин @langfuse/* 5.11.1) |
| Точки в именах MCP-тулов, префикс `workflow_*` против `flow_*` | 01,13,14 | DECISIONS «MCP-контракт» + CONVENTIONS |
| Имена пакетов `@app/llm`, `@app/stats` | 11,13 | CONVENTIONS: `@wf/<name>` |
| tenant_id + RLS в DDL embeddings и tool_effects | 09,16,17 | DECISIONS «Данные» |
| Единой strict-схемы на всех провайдеров нет; профиль на пару (схема × провайдер); запрет рекурсии | 05,11 | DECISIONS «Структурированный вывод» |
| Чекпоинт после каждого шага делает сам VoltAgent | 10 | 00-verified-by-lead + DECISIONS |
| Порог allowed-set ≤50 enum / выше — индексный выбор, потолок ~416 | 04,05 | DECISIONS «Структурированный вывод» п.4 (остался только замер, см. L-04) |
| Кто строит обратный индекс «ссылка → потребители» для R-05 (бывш. F-05) | 04,07 | ADR-0017: индексатор в схему `idx`; инвалидация — новый F-65 |
| Дельты как единица хранения версии спеки (бывш. L-08) | 04 | ADR-0017: история — git, дельта остаётся кэшем диффа |
| Хранение версий фрагментов промтов, `fragment@v` (бывш. L-10) | 08 | ADR-0017: версий в именах файлов нет, пин — `wf.lock.yaml` и `release_hash` |

## BLOCKER — закрыть до спайка фазы 0

Сортировка: сначала то, от чего зависит раскладка репозитория и форма IR, затем семантика рантайма,
затем фикстуры критериев остановки.

| ID | Вопрос | Док | Что сделать, чтобы закрыть | Усилие |
|---|---|---|---|---|
| B-01 | Замкнутый список `kind` узлов IR: 03 объявляет 5 примитивов + 7 комбинаторов, но в комплекте используются `call` (04), `load`/`retrieve` (09), `reduce` (03,06). Статус каждого — ядро, стандартная библиотека или сахар — не определён | 03,04,06,09,22 | Решение ведущего + закрытый enum `kind` в JSON Schema корня IR; для сахара — правило разворачивания до компиляции | 1 д |
| B-02 | Единого реестра имён пакетов `@wf/*` нет: `@wf/trace` (02) против `@wf/obs-otel`/`@wf/obs-langfuse` (12); `@wf/registries` (02) против `packages/types` (05); `@wf/api-types` (15) против `@wf/sdk` (20); `@wf/datasets`, `@wf/scorers`, `@wf/blame` (13) в дереве не выделены | 02,05,12,13,15,20 | Нормативная таблица пакетов в 20-repo-and-tooling.md, правка ссылок в 5 документах, CI-проверка имён | 3 ч |
| B-21 | Границы тенанта и проекта на файлах: монорепо со всеми проектами против репозитория на проект. RLS отвечала на это бесплатно, ФС не отвечает; от ответа зависят раскладка дерева, `wf check` в CI и §7 модели данных | 16,19,20 | ADR ведущего + строка в DECISIONS: что именно заменяет RLS как границу тенанта на ФС и где стоит проверка | 0.5 д |
| B-03 | Версия TypeScript нигде не зафиксирована: monorepo-quality даёт 6.0.3, determinism-export требует tsc 7.0.2 для проверки генерата, в DECISIONS строки нет | 20,21 | Решение ведущего (6.0.3 для репозитория, вопрос отдельной версии для standalone-генерата) + строка в DECISIONS | 2 ч |
| B-04 | Нижняя граница Node не зафиксирована: vitest 5 требует ≥22.12, promptfoo@0.123.0 — ≥22.22 | 13,20 | Решение: единый Node 22.x (какой минор) либо отдельный образ для @wf/model-matrix; строка в DECISIONS + матрица CI | 1 ч |
| B-05 | Библиотека Result на границах I/O не выбрана: true-myth 9.4.0 против neverthrow 8.2.0 (19 месяцев без релиза). Без неё нельзя писать обёртки веток и элементов, предписанные DECISIONS | 20 | Решение ведущего + строка в DECISIONS; критерий — типизация `Result<T,E>` в isolatedDeclarations | 2 ч |
| B-06 | `isolatedDeclarations` × Zod 4: переживают ли реэкспорты `z.infer<typeof S>` сборку tsdown 0.23 или в `.d.ts` уедет `unknown` | 20,21 | Спайк на @wf/ir: собрать tsdown-ом, проверить, что `.d.ts` содержит полные типы IR. При провале — отказ от isolatedDeclarations либо ручные декларации | 1 д |
| B-07 | Мультитенантность при процессном синглтоне `WorkflowRegistry` (`globalThis.___voltagent_workflow_registry`): изоляция префиксом в chainId против пула процессов на тенанта; утечка `activeExecutions`; сброс регистра между vitest-воркерами | 02,05,10 | Прототип «два тенанта в одном процессе» + ADR о модели процессов; тест на параллельные vitest-воркеры | 1 д |
| B-08 | Как auth-контекст доезжает до `filterWorkflows`/`filterTools` в @voltagent/mcp-server@2.2.0 — от этого зависит, один ли MCP-сервер на всех тенантов | 02,06,14 | Интеграционный тест «два тенанта, один процесс»: проверить, что фильтры видят контекст запроса, а не старт процесса | 0.5 д |
| B-09 | Точные имена экспортов ai@6.0.280: `embed`/`embedMany` (сигнатуры), частичный стриминг объектов (`partialOutputStream` против `partialObjectStream`), тип middleware для кассет (предположительно `LanguageModelV3Middleware`) | 05,09,10,13,16 | Прочитать `node_modules/ai/dist/index.d.ts`, зафиксировать в 11-providers.md таблицей «имя → сигнатура» | 2 ч |
| B-10 | Использует ли `Output.object` из ai@6 строгий режим провайдера (`json_schema` strict) или tool-based выдачу и какими ключами `providerOptions` это переключается | 05,10,11 | Прогон с записью сырого запроса через middleware на OpenAI и Anthropic; зафиксировать фикстуры запроса | 0.5 д |
| B-11 | Объём replay-кэша: переисполняет ли `timeTravel` вызовы моделей для шагов ДО `stepId` — кэш на каждом узле или только на целевом | 03,10 | Счётчик вызовов в middleware в режиме `replay_strict` на воркфлоу из 5 llm-узлов | 0.5 д |
| B-12 | Тип `concurrency` у `andForEach` — число или функция от контекста; от этого зависит компиляция `concurrency: all` из архетипа judge_panel | 03 | Грепнуть `dist/index.d.ts` @voltagent/core@2.10.0; при числе — компилятор подставляет `items.length` в момент сборки шага | 1 ч |
| B-13 | `andRace`: реальная отмена проигравших веток (в контексте шага нет abortSignal, есть только `ctx.state.signal`), тип агрегированной ошибки при провале всех веток (AggregateError или первая) и кто агрегирует стоимость проигравших | 03 | Пробный прогон с тремя ветками разной длительности: свой AbortController в контексте вызова + подсчёт usage проигравших | 0.5 д |
| B-14 | Семантика `ctx.bail(result)` внутри `andForEach`, `andAll` и тела `andDoUntil`: завершает весь воркфлоу или только вложенное исполнение | 10 | Прогон-проба на три случая; до ответа ранний выход из веера и цикла делаем через Result и правило остановки | 0.5 д |
| B-15 | Ключ кассеты: persistence требует PK `(cassette_id, request_hash, seq)` и несколько ответов по порядку, determinism-export требует «один ключ — один ответ» и падение `AMBIGUOUS_REPLAY`. Сводится к вопросу, входит ли номер итерации в нормализованный запрос | 16,18 | Решение на уровне нормативной семантики (включить attempt_scope в ключ и убрать seq либо оставить seq) + правка DDL §2.13 | 0.5 д |
| B-16 | Подпуть импорта @noble/hashes@2.4.0 (`/sha2` против `/sha256`) — пакета нет в probe, а на нём стоит весь @wf/canon | 18 | `npm view` + установка в probe, прочитать `exports` в package.json | 15 мин |
| B-17 | Код правила для непривязанного обязательного слота: R-09 (пример §9 спеки) против R-10 (каталог §8) | 07,22 | ADR ведущего, затем правка §3.1 компилятора, таблицы мутанта unbound-slot и примера в спеке | 1 ч |
| B-18 | Разделитель префикса при раскрытии компонента (`.`, `__`, `/`) не зафиксирован; от него зависят уникальность id, парсинг `wf.node_id` и группировка на канвасе | 22 | Решение ведущего + запись в 22-glossary.md и в правило генерации id компилятора | 1 ч |
| B-19 | Эталонный 15-шаговый воркфлоу не определён, а от него зависят kill-критерии 2, 3, 6, 9, 11, 13, 14, 15 | 21 | Собрать фикстуру (IR + датасет + ожидания) и положить в репозиторий как `fixtures/reference-15`; без неё критерии неизмеримы | 1 д |
| B-20 | Базовая линия времени ручной сборки для kill-8 не замерена, а замерять её нужно ДО старта фазы 0 (потом сравнивать не с чем) | 01,21 | Хронометраж двух ручных сборок эталонного воркфлоу (см. B-19) инженером без платформы, медиана в `gates/kill-8.json` | 1 д |

## PHASE-1 — закрыть до фазы 1

Сортировка: нормативные каталоги и контракты → провайдеры и схемы → промты и контекст → данные и
инфраструктура → безопасность → evals → фронтенд.

| ID | Вопрос | Док | Что сделать, чтобы закрыть | Усилие |
|---|---|---|---|---|
| F-01 | Уровни гарантии части правил не L1: R-13, R-46, R-47 (L2), R-26, R-28 (L3), R-74 (L1/L3). Компилятор проверяет только статическую часть — это не зафиксировано | 07 | Решение ведущего «декларация обязана существовать — L1-часть строки» + правка формулировок в §8 спеки | 2 ч |
| F-02 | Коды правил вне каталога: R-S1..R-S22 (security), подправило R-37a (бюджет allowed-set). `R-T13` и `R-G1..R-G4` закрыты решением ведущего: CONVENTIONS даёт `R-T1..R-T13` и `R-G1..R-G4`, реестры — 07 §3.2 и §3.7 | 07,19 | Один ADR на оставшиеся диапазоны семей + внесение в каталог §8/§9; альтернатива для R-37 — поле `subrule` | 2 ч |
| F-03 | Канонический список MCP-тулов не сведён: §13.1 не знает про `flow_patch` с `base_blob`, неясно, поглощает ли он `flow_set`/`flow_remove`; нет тулов операций над промтами и над путями в дереве; `type_*` и `dataset_*` не сверены (`human_task_*` из §13.1 отменены ADR-0017) | 01,05,07,08,13,14 | Итоговая таблица тулов в 14-mcp-contract.md + линтер имён в CI (snake_case, префикс группы, отсутствие синонимов) | 1 д |
| F-04 | Матрица «87 проблем каталога §8 → механизм → уровень гарантии → требование» не сведена; для L2–L4 нет проверки кроме G-exp/G-release | 01,07 | Свести таблицу в 07-compiler.md, для каждой строки указать механизм и тест | 2 д |
| F-06 | R-74: непересечение выборок проверяемо, факт «оптимизатор видел holdout» — нет | 07,13 | Контракт на декларацию областей видимости датасетов в 13-evals-and-gates.md | 4 ч |
| F-07 | Обязательность `maxLength` у каждой строки и `maxItems` у каждого массива в реестре типов: без них R-T9/R-12/R-C7 недоказуемы, кода у правила нет | 05,07 | Завести правило реестра в 04-ir-schema.md + код в каталоге | 2 ч |
| F-08 | Язык контрактов `requires`/`ensures` (distinct, families, disjoint) шире `$defs/predicate`; алгоритм проверки ограничений на тип-параметры дженериков не описан; асинхронные предикаты (существование id в реестре) — запрет или `superRefine` + `safeParseAsync` в zod 4.6.2 | 03,04,05 | Ввести `contract_predicate` как отдельную грамматику + прогон async superRefine на zod 4.6.2; решение «async в requires запрещён» предпочтительно | 2 д |
| F-09 | Форма `policies` и `defaults` задана по месту (§6.5 перечисляет виды, не форму); соотношение `views` типов и проекций pick/omit; реестр типов ошибок для ключей `try.catch`; покрывают ли источники `andMap` проекции flatten и map{select} | 04,06 | Свести схемы в 04-ir-schema.md вместе с 06-registries.md; для andMap — проба, что не покрыто, эмиттится в andThen | 1 д |
| F-10 | Канонизация IR против порядка полей: JCS сортирует ключи, jsonb порядок не хранит. Правило «порядок значим — значит массив» (`fields: [{name,type,description}]`) требует подтверждения генератором JSON Schema; что именно подаётся на хеш компонента и промта (домены `wf/component/v1`, `wf/prompt/v1`); хранение `ui` вне хеша | 04,22 | Тест «IR → JSON Schema → порядок полей провайдера» + фиксация доменов хеша в 04-ir-schema.md; DDL flow_layouts в 16-data-model.md | 4 ч |
| F-11 | Единая функция канонизации и доменной сепарации для ключей кассет, `canonical_sha256` элементов датасета и хешей экспорта не выделена | 13,18 | Пакет @wf/canon + тест на совпадение байтов по трём сценариям | 4 ч |
| F-12 | Лимиты схем Gemini (глубина, число свойств, размер enum, действие `propertyOrdering` в новом response_format) не публикуются — профиль gemini-strict нельзя проверять как openai-strict, бюджет блока output_format нечем считать | 05,07,08,11 | Эмпирический бинарный поиск границ на живом API, результат — таблица в 11-providers.md и профиль в компиляторе | 1 д |
| F-13 | Поддерживает ли OpenAI strict `minLength`/`maxLength` для базовых (не fine-tuned) моделей — в списке поддерживаемых не назван, профиль сейчас вырезает всегда | 05,11 | Проба схемой с minLength на gpt-базовой модели; при поддержке — снять вырезание | 2 ч |
| F-14 | `structured_outputs` у OpenRouter — объявление, а не гарантия; каталог возможностей для `capability_probes` (список, периодичность, бюджет) не определён | 06,11,16 | Схема-ловушка на пару (модель × endpoint), флаг `strict_json_verified` в caps, `probe_version = 1`, список проб из 05-type-system.md | 1 д |
| F-15 | Какие поля usage приходят через ai@6 на каждом провайдере (кэшированные и reasoning-токены), точные имена полей стоимости и кэша в `providerMetadata` — раздел остался TODO | 06,11 | Живые вызовы на OpenAI/Anthropic/OpenRouter + фикстуры с Zod-схемой + таблица фактической стоимости прогона | 1 д |
| F-16 | Единица `pricing.input/output` у Together: OpenAPI говорит «per token», значение 0.3 для 13B по величине = $/1M; дамп `GET /v1/models` не снят, поля не сверены с CostEntry | 06,11 | Вызов с известным числом токенов и сверка со счётом; снять дамп каталога в фикстуру | 4 ч |
| F-17 | Таблица соответствий id моделей между источниками (`app.model_key_aliases`) не составлена — данные models.dev не приклеиваются к каталогу, строковая склейка запрещена. Источник цен и процедура обновления каталога влияют на planHash | 06,07,11 | Составить таблицу алиасов + правило версионирования каталога (ревизии синка, `pricing.overrides`, политика для :free-моделей, алерт «цена изменилась с момента компиляции») | 1 д |
| F-18 | Калибровка токенизатора: `token_ratio` (1.15 латиница / 1.3 кириллица и CJK) снят на трёх фрагментах; `context_safety_ratio` 0.9 не обоснован | 07,08,09 | ≥1000 реальных вызовов на профиль, коэффициент = квантиль 0.99 отношения факт/оценка; колонка в каталоге моделей + ADR на context_safety_ratio | 2 д |
| F-19 | Точный счёт токенов для Claude: локального токенизатора нет, эндпоинт `/v1/messages/count_tokens` помечен UNVERIFIED (сигнатура, лимиты, сетевой) | 07,08 | Подтвердить по докам Anthropic + решение, вызывается ли он на публикации версии промта и в офлайн-калибраторе | 2 ч |
| F-20 | Белый список фильтров liquidjs не зафиксирован («≈8–10»), список join/size/strip/upcase/downcase/capitalize/truncate/escape — предложение; `unless` и оператор `contains` оставлены со знаком вопроса | 08 | Прогон по шаблонам архетипов §7.5, затем один ADR на белый список и на запрет unless/contains | 4 ч |
| F-21 | Где обязан стоять `{{ output_format }}` — в system или в последнем сообщении; сейчас допустимы оба | 08 | Замер на golden-наборе на двух провайдерах, одно решение для всех архетипов | 1 д |
| F-22 | Классы видимости полей: `hidden` введён как бинарный признак, спека упоминает политики видимости (R-T10) без перечня классов; формат политик не определён | 05,08 | Перечень классов + формат политики узла в 08-prompts.md, правило R-T10 переформулировать через него | 4 ч |
| F-23 | Контракт `SlotRange[]` от рендерера промтов в заметках не описан, а на нём держится провенанс слотов в спане и подсветка в Studio | 08,12 | Зафиксировать тип и инварианты в 08-prompts.md | 2 ч |
| F-24 | Поведение `hnsw.iterative_scan` на pgvector 0.8.6 не воспроизводилось (взято из changelog 0.8.0) | 09,16 | Интеграционный тест на testcontainers 12.1: 100k векторов, фильтр 1%, сравнение off / relaxed_order / strict_order по числу строк и recall | 1 д |
| F-25 | Ключ партиционирования `app.embeddings` (tenant_id против project_id) и стоимость HNSW-индекса на каждой партиции | 09,16 | Замер на 20 партициях × 50k векторов (время сборки индекса, размер, recall), решение + правка DDL | 1 д |
| F-26 | Модель эмбеддингов и размерность не выбраны (`vector(1536)` взято из наброска), halfvec не рассмотрен; интерфейс `BaseRetriever` в @voltagent/core 2.10 не читался; при `memory.scope: tenant` фильтрация исполняется внутри VectorAdapter (контракт, не гарантия), кросс-тредового поиска нет | 06,09,16 | Выбрать модель → замер recall vector против halfvec → решение по своему ретриверу поверх pgvector; грепнуть Retriever в dist/index.d.ts | 2 д |
| F-27 | Rerank: модель, провайдер и бюджет не выбраны (в спеке только флаг); кто утверждает порог recall@k и что делает гейт при его отсутствии | 09,13 | Сравнение двух кандидатов по recall@k_full и латентности, внести в DECISIONS; правило гейта — в 13-evals-and-gates.md | 1 д |
| F-28 | Форма провенанса слотов в спане: параллельные массивы `wf.slot.*` (фильтруемы) против одной строки `wf.slot.provenance_json` (не упирается в лимит ~128 атрибутов) | 09,12 | Прототип на узле с 40 слотами в Langfuse 5.11.1: проверить, умеет ли бэкенд фильтровать по элементу массива атрибутов | 4 ч |
| F-29 | Двойной учёт usage и cost при одновременном экспорте спанов в VoltOps и Langfuse | 02,12 | Прогон с двумя процессорами спанов, сверка с нашей строкой `run_budget` | 4 ч |
| F-30 | Langfuse: точный OTLP-endpoint (`/api/public/otel/v1/traces`) не подтверждён из кода SDK; форма ответа `langfuse.api.trace.get` (observations, usage, cost) не проверена; достаточность API experiments для парных наблюдений по `case_id` | 12,13,15 | Снять фактический запрос на self-host + один живой вызов trace.get + проверка зеркалирования scores | 4 ч |
| F-31 | API @voltagent/resumable-streams@2.0.1 (README пуст): возобновляется ли SSE по Last-Event-ID; где исполняется интерактивный прогон при нескольких репликах ROLE=api и что с прогоном при рестарте реплики | 02,15 | Прочитать `dist/*.d.ts` + e2e «релоад вкладки посреди прогона» + решение по sticky-сессиям; до закрытия — fallback `startAsync()` + REST-поллинг | 1 д |
| F-32 | Документация VoltAgent противоречит сама себе про WebSocket: `api/streaming.md` — «WebSockets are currently unauthenticated», `api/authentication.md` — предлагает `?key=` / `?token=` | 15,19 | Проверка на собранном сервере, применяется ли authNext к `/ws/*`, + ADR; консервативно `/ws/*` наружу не выставляем | 4 ч |
| F-33 | GET-эндпоинты истории исполнений @voltagent/server-hono публичны по умолчанию; не подтверждено, читает ли `isDevRequest` в @voltagent/server-core@2.1.20 `process.env.NODE_ENV` в момент запроса или на старте | 02,19 | Middleware в `configureApp` + тест 401 на каждый GET; прочитать реализацию isDevRequest и закрыть e2e-тестом | 4 ч |
| F-34 | У better-auth@1.7.4 нет экспортов `plugins/api-key`, `plugins/mcp`, `plugins/oidc-provider`; OAuth-provider для MCP не подтверждён — способ выдачи долгоживущего токена агенту не зафиксирован | 14,19 | Прочитать exports пакета, выбрать: свой api-key поверх сессий или внешний провайдер; до закрытия — долгоживущий API-токен | 1 д |
| F-35 | Поведение Claude Code при `notifications/tools/list_changed` в середине сессии не проверено, а на нём держится вся стратегия фазового раскрытия; доступен ли Tool Search Tool для MCP-тулов; поддержка AGENTS.md не подтверждена | 14 | Проба на живом клиенте: включить/выключить группу тулов в середине сессии и посмотреть, перечитывает ли клиент список | 4 ч |
| F-36 | Дублирует ли @modelcontextprotocol/sdk@1.30.0 `structuredContent` в `content[0].text` автоматически — если да, ответ уходит удвоенным и бюджет ответа съедается вдвое | 14 | Прочитать сериализацию в SDK + один вызов с записью байтов ответа | 1 ч |
| F-37 | `dry_run` флагом `flow_patch` или отдельный тул `flow_validate`; порог «≤20–25 активных тулов» и оценка 250–600 токенов на определение — расчётные, не измеренные | 02,14 | Замер реального размера определений тулов в токенах + A/B на точность выбора тула; решение зафиксировать в 14-mcp-contract.md | 4 ч |
| F-38 | Формальная таблица коммутативности пар операций `op × op` для автоматического ребейза `flow_patch` при `STALE_FILE` не составлена | 02,07,14 | Составить таблицу + property-тесты на fast-check; до неё любой конфликт по пересекающимся путям возвращается как `rebase: manual` | 2 д |
| F-39 | pg-boss 12: PgBouncer в transaction pooling при `useListenNotify:false` (поллинг не проверен на нашем пуле); нагрузка при `partition:true` на объёме eval-батчей; включать ли `notify:true` на очереди human.deadline | 17 | Стенд: pool + pg-boss + батч на 10k job-ов, замер latency и нагрузки на БД; триггеры перехода на graphile-worker пересчитать по факту | 1 д |
| F-40 | Поведение `resumeSuspendedWorkflow` при незарегистрированном `workflowId` (null или throw) — от этого зависит обработчик human.deadline; версионный `registryId` `${workflowId}@${content_hash}`: возобновляется ли исполнение, приостановленное под другим хешем содержимого | 17 | Два сценария на стенде, зафиксировать ветвление в обработчике | 4 ч |
| F-41 | Контракт доставки `attempt_scope` (номер итерации цикла из workflowState) в ToolContext не зафиксирован | 07,17 | Описать в 07-compiler.md, как компилятор прокидывает scope в вызов тула; связано с B-15 (ключ кассеты) | 2 ч |
| F-42 | При каком числе шагов и объёме events перезапись всей строки исполнения становится узким местом; GIN-индекс по metadata в `voltagent_memory_workflow_states` отсутствует в пакете | 10 | Нагрузочный прогон 500 шагов (размер строки, время UPDATE) + добавить GIN в наши миграции и проверить отсутствие конфликта с рантайм-DDL адаптера в схеме voltagent | 4 ч |
| F-43 | Кластерный лимитер: шардирование квоты по числу воркеров против общего счётчика в Postgres/Redis | 10 | ADR с замером точности и латентности на 4 воркерах | 4 ч |
| F-44 | RLS: план политики через подзапрос `project_id IN (SELECT ... WHERE tenant_id = ...)` не измерялся; цена составного FK `(project_id, tenant_id)`; порядок tenant_id первым в составных индексах нарушен в наброске `runs_project_started_idx` | 16,19 | `EXPLAIN (ANALYZE, BUFFERS)` на run_nodes с реалистичным объёмом: батч-вставка и лента прогонов под ролью app_user; поправить индексы | 1 д |
| F-45 | Политика `checkpointInterval` по классу воркфлоу (цена running-чекпоинта после каждого шага на цепочках дешёвых шагов); поведение чекпоинтов и `getStepResult` при трёхуровневой вложенности `andWorkflow` с приостановкой на нижнем уровне — это же граница инлайна компонента | 02,03 | Замер стоимости чекпоинта на 100 дешёвых шагах + проба вложенности на три уровня | 4 ч |
| F-46 | Работает ли Workspace / LocalSandbox (EXPERIMENTAL) для шага воркфлоу без агента и применим ли его `toolConfig` — от этого зависит, чем изолируются code-узлы; версия и лицензия @voltagent/cli (нужен для CI-гейта evals) не проверены | 02,06,12,19 | Прототип шага с Workspace без агента + `npm view @voltagent/cli`; до ответа Workspace в v1 не входит, политики держим в своих хуках | 1 д |
| F-47 | Детектор PII для русскоязычных форматов (СНИЛС, ИНН, паспорт, полис, IBAN, ФИО, адреса) — библиотека не выбрана; встроенные гардрейлы покрывают только e-mail/телефоны/длинные числа и типизированы как `OutputGuardrail<string>` | 19 | Обзор двух-трёх библиотек + замер precision/recall на своём наборе; при отсутствии — свои правила поверх `createPIIInputGuardrail` | 2 д |
| F-48 | Модель и порог LLM-классификатора инъекций (R-S9) не определены | 19 | Состязательный датасет + замер precision/recall на трёх дешёвых моделях, порог в конфиг политики | 3 д |
| F-49 | Хранилище секретов для prod не выбрано: есть контракт `SecretResolver` и адаптеры env/file, конкретный менеджер (Vault, SOPS+age, облачный KMS) не исследован | 19 | Выбор по критериям (ротация, аудит, цена), адаптер + строка в DECISIONS | 1 д |
| F-50 | Кассеты на порту тула: список тулов с побочными эффектами и политика в `run_mode=replay` не определены | 12,18 | Перечислить тулы по `effect_class` (read/write/external) и задать политику replay для каждого класса | 4 ч |
| F-51 | Состав env конформанс-кейса: отдельный файл `conformance/env/<caseId>.json` с `randomStream` против env внутри кейса с `seed` | 18 | Решение + зафиксированный алгоритм PRNG в semantics/00-model.md (иначе seed невоспроизводим) | 4 ч |
| F-52 | Стабильность интроспекции Zod 4.6.2 (`._zod.def` / `.def`) для извлечения доменов enum, дискриминантов union и числовых границ в стратегии from_types (нужно для kill-9) | 13,21 | Характеризующий тест + адаптер `SchemaDomainReader`, чтобы пин мажора ловился одним тестом | 4 ч |
| F-53 | Две версии autoevals в дереве: @voltagent/scorers@2.1.0 тянет `autoevals ^0.0.131`, явная установка 0.3.0 ради LLMClassifierFromSpec/ModelGradedSpec/Battle даст дубликат; поддержка не-OpenAI провайдеров в 0.3.0 не проверена | 13 | Проверить реэкспорт из VoltAgent + прогнать Factuality через наш провайдерный слой на Anthropic-модели | 4 ч |
| F-54 | Производительность BCa не замерена: 60 тестов × 10000 реплик × n=500 на Node | 13 | Бенчмарк; при >30 с — вынести в воркер и кешировать jackknife | 4 ч |
| F-55 | Относятся ли «минимум 200 элементов» из DECISIONS к калибровке судьи или только к блокирующему гейту; порог 0.70 назначен по аналогии (Landis & Koch) и на наших данных не проверялся | 01,13 | Явное правило в 13-evals-and-gates.md + пересчёт QWK и Krippendorff alpha после первых 100 размеченных примеров | 4 ч + разметка |
| F-56 | Пустые `datasetId` / `datasetVersionId` в `ExperimentItemResult` при дескрипторе `{resolver}` (источник истины датасетов — наш Postgres) | 13 | Проверить, что агрегаты `ExperimentSummary` не ломаются; при поломке — свой агрегатор | 2 ч |
| F-57 | Live-скоринг на уровне узла воркфлоу: `eval:{scorers}` есть только у Agent, гипотеза `WorkflowHooks.onStepEnd` + `runLocalScorers()` не проверена | 12 | Прототип хука с локальным скорером на одном узле | 4 ч |
| F-58 | Фронтенд-спайки одним блоком: CodeMirror 6 в Next 16.3 (подсветка слотов, автодополнение, setDiagnostics); @rjsf/shadcn@6.10.0 под Tailwind v4 и переваривает ли RJSF `oneOf` из `z.toJSONSchema()` на discriminated unions; `new Worker(new URL('elkjs/lib/elk-worker.min.js', import.meta.url))` в сборке Turbopack; водопад Recharts против своего CSS-компонента на 1000 спанов; замеры React Flow на 200–500 узлах, ELK на плотном графе (рёбер ≈ 4× узлов), вес CodeMirror после минификации | 12,15 | Один скелет приложения, шесть замеров подряд; фолбэк elkjs через `public/` уже рабочий | 4 д |
| F-59 | Источник `confidence` для cascade (поле в типе выхода, детерминированный скорер или отдельный судья — logprobs доступны не везде); компилируется ли `bounded_agent` в один агентский шаг с внутренним лимитом или в явный loop | 03 | Решение в 03-core-language.md + проба обоих вариантов bounded_agent на одном воркфлоу | 1 д |
| F-60 | Граница между `verify: [judge]` и правилами судей R-J1..R-J6 | 09 | Согласовать в 09-context-model.md, при необходимости убрать judge из VerifyKind | 2 ч |
| F-61 | Канонический формат Money: список валют ISO-4217 и правило рендера не заданы; в 16-data-model.md соседствуют `numeric(18,8)` и `numeric(18,10)` без правила | 05,16,22 | Одно правило точности по таблицам + expand-миграция + формат Money в реестре типов | 4 ч |
| F-62 | `pg_partman` в образе pgvector/pgvector:0.8.6-pg18 не проверен; берёт ли `drizzle-kit migrate` advisory lock | 16 | `\dx` в контейнере + чтение исходника drizzle-kit; если lock не берётся — обернуть в `pg_advisory_lock` | 1 ч |
| F-63 | Имя и версия GH-экшена установки pnpm 12 (`pnpm/setup@v1` против `pnpm/action-setup@v4`) не подтверждены | 20,21 | Прогон workflow в CI на обеих версиях, оставить рабочую | 30 мин |
| F-64 | Журнал `renames` в `project.yaml`: формат записи, кто дописывает (только `flow_patch` или и watcher при `git mv`), как резолвится lineage прогонов и кассет по цепочке переименований и когда журнал усекается | 04,16 | Схема записи + резолвер в 16-data-model.md, property-тест «цепочка из 5 переименований не рвёт lineage» | 1 д |
| F-65 | Дрейф индекса: как обнаруживается расхождение схемы `idx` с деревом (watcher, счётчик поколений, сверка хешей), когда переиндексация инкрементальная, а когда полная, и что отдаёт продукт при `index_stale` | 02,16 | Контракт индексатора в 16-data-model.md + CI-джоба «переиндексация с нуля даёт те же хеши» | 1 д |
| F-66 | Дисциплина прямой правки не описана как набор проверок: что именно блокирует хук `flow_check` на `PostToolUse`, что блокирует `wf check` в CI, а что только предупреждает, и что происходит с файлом, записанным в обход хука | 14,20 | Таблица «диагностика → где ловится → что делает» в 20-repo-and-tooling.md; тест «правка мимо хука ловится на pre-commit» | 4 ч |

## LATER — решается по ходу

| ID | Вопрос | Док | Что сделать, чтобы закрыть | Усилие |
|---|---|---|---|---|
| L-01 | Остаточный риск пина OpenRouter: линия 2.x в maintenance с 2026-08-14 | 11 | Ежемесячный контроль патчей + план Б (переход на 3.x вместе с ai@7) в коде за фича-флагом | 2 ч/мес |
| L-02 | Регламент при breaking change VoltAgent (Workflows в статусе Preview, пин 2.10.0): откат пина или правка адаптера при падении конформанс-набора | 01,18 | Записать процедуру в 18-export-and-conformance.md | 2 ч |
| L-03 | Lineage: top-level поля против metadata — на живом коде не проверено | 10,21 | Прогон «форк → рестарт → чтение обоих путей» + линтер на запрет доступа к top-level lineage-полям | 4 ч |
| L-04 | Порог allowed-set: DECISIONS даёт ≤50 enum / выше индексный выбор, research §4.5 — честный enum до 400, §4.4 предупреждает о позиционном смещении при индексном выборе | 05 | Замер точности на N = 60, 150, 400; решение DECISIONS действует до результата, менять только ADR | 1 д |
| L-05 | Распространяется ли негарантированный регистр Anthropic на `const` дискриминанта union, а не только на enum | 05 | 50 вызовов на схеме с дискриминантом, сверка регистра | 2 ч |
| L-06 | По какой схеме валидируется каждый элемент слота `examples` (`Example<In,Out>[]`) | 05 | Правило в реестре типов | 2 ч |
| L-07 | Нужен ли парсер авторской строковой формы предиката (`$.score >= 0.8`) или Claude сразу пишет структурный предикат | 04 | Прототип `flow_patch`: посмотреть, что реально пишет модель | 4 ч |
| L-09 | Верхняя граница `max_iter = 50` — инженерный предохранитель, продуктового порога нет | 04 | Замер на первых воркфлоу + правило R-L1 | 2 ч |
| L-11 | Свежесть графовых пакетов ядра доверия: graphology-dag@0.4.1 (2023-12), graphology-traversal@0.3.1 (2022-05) | 07 | Решение: вендорить ~300 строк сразу или держать зависимости с тестами-характеризациями | 4 ч |
| L-12 | Целевые бюджеты времени компиляции сформулированы качественно | 07 | Бенчмарк на фикстуре 100 узлов / 30 промтов + регрессионный гейт в CI | 1 д |
| L-13 | Батч-API OpenAI и Anthropic (лимиты, формат, поведение при ошибке) не исследовались; §9 описан только для Together | 11 | Прочитать доки, добавить адаптеры в @wf/llm при появлении спроса | 1 д |
| L-14 | Self-hosted профили: сохранён ли алиас `guided_json` в vLLM после переезда на structured_outputs; матрица бэкендов SGLang (XGrammar/Outlines/Llguidance) не выверена; время компиляции грамматики XGrammar на enum ~10k альтернатив не замерялось | 11 | Проверка на двух версиях сервера в testcontainers; до сверки SGLang не допускаем в профили с strict_json | 1 д |
| L-15 | Очереди, второй эшелон: зрелость Postgres-бэкенда BullMQ 6.3.4 (умеет ли enqueue в чужой транзакции), триггеры перехода на graphile-worker выставлены оценочно, UI для pg-boss 12 не найден, семантика `group.tier` относительно groupConcurrency, запускается ли зависимый job отчёта при деградации части родителей в deadletter, поведение rrule-temporal@^2.2 с pg-boss при переводе часов | 17 | Разбирать по мере появления симптомов; UI — своя страница в Studio | 2 д |
| L-16 | Сэмплирование трасс в проде не определено (parentbased_always_on = 100%), объём ClickHouse может быть неподъёмным; ретеншн трасс без EE (Data Retention Policies — EE-фича); пороги алертов §9.4 стартовые | 12 | Замер объёма после первого месяца → правило сэмплирования + TTL на ClickHouse своими руками или покупка EE | 1 д |
| L-17 | Лицензионный статус `blobStorageIntegrations` (batch-экспорт трасс в S3): OSS или EE — это путь резервного вывоза трасс | 12,13 | Проверить по лицензии репозитория Langfuse | 1 ч |
| L-18 | `UNIQUE (project_id, sha256)` в app.blobs закрывает оракул межтенантного дедупа, но теряет экономию между проектами одного тенанта; сверка `blobs.ref_count` с реальными ссылками — ручной аудит, ломается при новой колонке `*_ref` | 16,19 | Замер доли повторов после первого месяца; реестр ссылающихся колонок в коде либо таблица `blob_refs` вместо счётчика | 1 д |
| L-19 | Теги SeaweedFS и Garage не проверены (строка в compose появится только с проверенным пином); Relations v2 (`defineRelations`) в drizzle-orm 0.45.2 отсутствует | 16 | Проверить теги перед вводом объектного хранилища; оценить миграцию relations при обновлении мажора | 4 ч |
| L-20 | Инструментальные хвосты: @stryker-mutator/typescript-checker@10 на `isolatedDeclarations` + `erasableSyntaxOnly` вживую не проверен (возможны ложные CompileError); порог мутантов задан только как `total >= 50` без минимума на класс (предложено `byClass[*].total >= 3`); тип удалённого кэша Turbo не выбран и не решено, кто держит `TURBO_REMOTE_CACHE_SIGNATURE_KEY`; входит ли @wf/canon в linked-группу формата бандла; дата выхода TypeScript 7.1 со стабильным программным API неизвестна | 20,21 | Разбирать по одному при настройке CI; TS 7.1 — внешний фактор, только мониторинг | 2 д |
| L-21 | Псевдонимизация PII в бандлах: список типов, детерминированный алгоритм замены, обратимость и место хранения обратной таблицы | 18 | Проектное решение вместе с F-47 (детектор PII) | 1 д |
| L-22 | Экспорт, мелочи формата: вводить ли `manifestHash` для полей вне merkleRoot или отложить до подписи в v2; пороги 2 КБ (value/valueHash) и 256 КБ (вынос тела кассеты в blobs/) не подтверждены замерами; политика при несовпадении мажора `formatVersion` (конвертер в бандле или в CLI); порог отказа импорта для minor/patch; точные пути и имена полей prior art (WebAssembly spec, JSON Schema Test Suite, CommonMark, sqllogictest) | 18,22 | Закрывать пакетом перед фиксацией formatVersion 1.0.0 | 1 д |
| L-23 | `quorum(k)` через барьерные каналы LangGraph (NamedBarrierValue/DynamicBarrierValue + reducer) не прототипирован | 18 | Прототип в рамках стресс-теста семантики (LangGraph — вторая цель миграции) | 1 д |
| L-24 | Метрика разнообразия уровня Vendi требует eigen-разложения в TS, свежесть ml-matrix не проверена; не проверено, появился ли автоматический blame с минимизацией в свежих LangSmith/Braintrust | 13,21 | Обзор + при отсутствии готового — своя реализация eigen на симметричной матрице | 1 д |
| L-25 | Метрики у потолка (accuracy 0.98): абсолютный MPE в процентных пунктах бессмыслен | 13 | Ввести режим `relative_error_reduction` и правило выбора между режимами | 4 ч |
| L-26 | Нет машиночитаемой версии глоссария, расхождение кода с таблицей ничем не ловится; идентификаторы «страж»/«матрица стадий»/«критик-цикл» (guard, stage matrix, critic_reviser) взяты из соседних документов, а не из спеки | 22 | `packages/contracts/glossary.json` + CI-проверка идентификаторов | 1 д |
| L-27 | Studio, хвосты: `experimental.useOffline` в проде не проверен; миграция на `cacheComponents: true` требует Suspense над каждым чтением сессии; `@tanstack/table-core/experimental-worker-plugin` помечен experimental; точные поля `nodeData` в json-edit-react@1.30.x не сверены; какой form-адаптер стоит по умолчанию в shadcn `form` (new-york); Storybook 9 + Next 16.3 + Tailwind v4 не проверены | 15 | Каждое — перед первой реализацией соответствующего экрана; worker-plugin не класть в критический путь v1; вместо Storybook — страница `/dev/gallery` | 1 д |
| L-28 | Роли `app_migrator`/`app_analytics` с BYPASSRLS обходят RLS; хеш-цепочка журнала делает вмешательство обнаруживаемым, но не предотвращает | 19 | Решение про внешний WORM-приёмник журнала для энтерпрайза (не v1) | 4 ч |
| L-29 | Ретенция цепочек `derived_from[]` (lineage) при циклах | 09 | Оценить объём на прогоне с `max_iter = 10`, задать ретенцию вместе с партициями run_nodes (см. PR-04) | 4 ч |

## PRODUCT — вопросы к владельцу продукта

| ID | Вопрос | Док | Что нужно от владельца | Срок |
|---|---|---|---|---|
| PR-01 | Пороги kill-критериев не заданы числами: N итераций в K2, X% экономии и ε неинфериорности в K10, допустимая доля исхода `truncated` в K4 (сам K4 требует 0 невалидных, но truncated — третий исход), целевой порог метрики «доля логики вне архетипов» | 01,21 | Числа или согласие на «медиана трёх пилотов + 50%» как правило вывода; фиксация в `gates/kill-*.json` | до старта фазы 0 |
| PR-02 | Мультитенантность: спека объявляет SaaS не-целью v1, DECISIONS требует tenant_id + RLS с первого дня | 01,05,16 | Утвердить ADR «модель данных мультитенантна, продуктовая поверхность — нет» либо снять требование | до старта фазы 0 |
| PR-03 | Лицензия elkjs — EPL-2.0 OR GPL-3.0-or-later (проверено первоисточником), в DECISIONS строка «Канвас» помечена MIT. Замены с вложенностью И портами под MIT нет | 12,15 | Юридическое заключение до начала работ над канвасом + ADR на правку DECISIONS; отказ означает отказ от автораскладки графа | до фазы 1 |
| PR-04 | Сроки ретеншна не заданы: партиции run_nodes (90/400 — заглушка), checkpoints (30/180), блобы зон 2 и 3, записи tool_effects, трассы | 09,12,16,17 | Решение по срокам хранения (стоимость против разбора инцидентов); до решения джоб удаления не включается | после первого месяца нагрузки |
| PR-05 | Протокол исполнения kill-14 не описан (какая модель, доступ к бандлу, число попыток) — критерий неповторим; трактовка «100% кейсов» уточнена документом до «100% tier=required в strict-режиме кассеты» | 18,21 | Утвердить протокол и трактовку | до фазы 1 |
| PR-06 | Второй MCP-клиент для kill-3 не выбран | 21 | Назвать клиента (от него зависит, что считается совместимостью) | до фазы 1 |
| PR-07 | Полномочия фонового встроенного улучшателя: может ли тратить бюджет моделей без аппрува и с каким лимитом | 01,19 | Лимит в деньгах и правило аппрува | до фазы 1 |
| PR-08 | Бюджет живых судей в проде (доля прогонов под live-скорерами, потолок расходов) не задан ни спекой, ни заметками | 13 | Доля и потолок | до фазы 1 |
| PR-09 | Момент закрытия R2: требование включает правку графа, риск §19 «Объём UI» переносит правку за фазу 0 (только вьюер); порядок фаз для kill-12 — проверять ли раннюю версию в конце фазы 1 | 01,21 | Решение по объёму UI фазы 0 и по порядку проверки kill-12 | до старта фазы 0 |
| PR-10 | У R24 нет собственного технического kill-критерия, K8 и K2 работают как прокси | 01 | Либо принять прокси, либо сформулировать продуктовый критерий | до фазы 1 |
| PR-11 | Мультивалютность не описана: канон USD, но полей `currency` и `fx_rate_at` нигде нет | 22 | Либо явная запись «только USD» в DECISIONS, либо ADR на мультивалютность | до фазы 1 |
| PR-12 | React Flow Pro ($169 разово за темплейт AI workflow editor как референс) — техблокера нет | 15 | Покупать или нет | до фазы 1 |
| PR-13 | Обоснование ветки `default` в `switch`: кто его принимает и блокирует ли его отсутствие компиляцию или только публикацию | 03 | Правило процесса: жёсткость проверки полноты enum | до фазы 1 |

## Регламент реестра

- Каждая запись закрывается либо ADR в `docs/adr/`, либо строкой в DECISIONS.md, либо тестом в репозитории.
  Закрытая запись не удаляется — переносится в конец таблицы с пометкой «закрыт: <ссылка>».
- Новый вопрос из документа комплекта попадает сюда с тем же ID-префиксом класса и ссылкой на документ.
- Класс повышается (LATER → PHASE-1 → BLOCKER) только вместе с указанием, какая работа встала.
