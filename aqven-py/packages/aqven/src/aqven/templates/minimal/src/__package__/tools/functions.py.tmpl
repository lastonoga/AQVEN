from typing import Annotated

from aqven.runtime import ToolContext
from pydantic import StringConstraints

from __package__.types import CountWordsOut


async def count_words(ctx: ToolContext, text: Annotated[str, StringConstraints(max_length=2000)]) -> CountWordsOut:
    return CountWordsOut(words=len(text.split()))
