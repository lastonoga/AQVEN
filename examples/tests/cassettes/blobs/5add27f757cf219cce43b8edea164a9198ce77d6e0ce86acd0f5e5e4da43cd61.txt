The tool expects amount: string? But the error says "invalid arguments for tool issue_store_credit: 1 validation error for Money". Wait, the tool signature: issue_store_credit = (_: { amount: string, customer_id: string, order_id: string }) => any. But the error indicates that the amount is being validated as Money type. That suggests the tool expects amount to be Money object? Wait, the error says "validation error for Money". So maybe the tool expects amount to be Money type, not string. But the signature says amount: string. But the error indicates that the tool expects a Money object. Let's re-read the tool signature: type issue_store_credit = (_: { // Credit amount amount: string, // The customer the credit is issued to customer_id: string, // The order the credit is issued against order_id: string, }) => any; So amount is string. But the error says "validation error for Money". That suggests the tool's internal validation expects Money type. Maybe the tool expects amount to be a Money object? But the signature says string. There is a mismatch. We need to figure out correct usage.

Let's try passing amount as "2000" maybe? But still string. The error says "validation error for Money". That might be because the tool expects amount to be a Money object? Let's try passing amount as "2000" or "2000.00"? But still string. The error indicates that the tool expects Money type. Let's try passing amount as "2000" but maybe the tool expects a Money object. Let's try passing amount as "2000" but maybe we need to pass amount as "2000" but the tool expects Money type. Let's try passing amount as "2000" but the tool expects Money type. Let's try passing amount as "2000" but maybe we need to pass amount as "2000" but the tool expects Money type. This is confusing.

Let's inspect the error: "invalid arguments for tool issue_store_credit: 1 validation error for Money Input should be a valid dictionary or instance of Money [type=model_type, input_value='20.00', input_type=str]". So the tool is expecting a Money type for the amount. That means the tool signature might be wrong. The tool expects amount to be Money type. So we need to pass amount as a Money object: {"amount_minor": 2000, "currency": "eur"}? But the tool signature says amount: string. But the error indicates Money. So we need to pass amount as Money object. Let's try passing amount as {"amount_minor":2000,"currency":"eur"}. But the tool signature expects amount: string. But maybe the tool's actual implementation expects Money type. Let's try passing amount as {"amount_minor":2000,"currency":"eur"}.

But the tool signature might be wrong. Let's try.